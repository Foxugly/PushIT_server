[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
markers =
    integration: end-to-end integration tests with fresh DB and local server
filterwarnings =
    ignore:'_UnionGenericAlias' is deprecated and slated for removal i